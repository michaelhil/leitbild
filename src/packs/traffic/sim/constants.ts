import type { AdapterId, PackId } from '../../../core/model/index.ts'
import { trafficPackId } from '../model.ts'

export const trafficSimRuntimeId = 'traffic-local'
export const trafficSimAdapterId = 'adapter:traffic-local' as AdapterId
export const trafficSimPackId = trafficPackId as PackId

