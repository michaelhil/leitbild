import type { AdapterId } from '../../../core/model/index.ts'
import { dronePackId } from '../model.ts'

export const droneNativeRuntimeId = 'drone.native'
export const droneNativeAdapterId = 'adapter:drone-native' as AdapterId
export const droneNativePackId = dronePackId
