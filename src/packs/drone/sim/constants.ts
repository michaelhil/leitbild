import type { AdapterId, PackId } from '../../../core/model/index.ts'

export const droneSimRuntimeId = 'drone.local'
export const droneSimAdapterId = 'adapter:drone-local' as AdapterId
export const droneSimPackId = 'drone' as PackId
