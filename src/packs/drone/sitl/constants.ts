import type { AdapterId } from '../../../core/model/index.ts'
import { dronePackId } from '../model.ts'

export const droneSitlRuntimeId = 'drone.sitl'
export const droneSitlAdapterId = 'adapter:drone-sitl' as AdapterId
export const droneSitlPackId = dronePackId
