# Optional World-specific settings for /etc/leitbild/world.env.
# WORKSPACE_HOST_URL is owned by leitbild-world.service. Do not override it here.
# The production Module talks to the loopback Host, not a public or retired domain.
