# Workspace Templates are optional apply-once factories

A Workspace Template may declare Module requirements and Module-owned seed data, but applying it produces an ordinary mutable Workspace with no ongoing controller relationship. Templates exclude Agent Profiles, prompts, grants, workflows, concrete Resource links, and reconciliation so repeatable environments do not grow into a generic orchestration platform; implementation waits for two demonstrated repeated setups.
