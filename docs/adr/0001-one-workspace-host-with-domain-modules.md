# Use one Workspace Host with isolated domain Modules

The product has one public Workspace Host for identity, Module Membership, navigation, and routing, while Microworld, Collaboration, Agents, and future Modules own their domain state and may run as separate processes. This removes duplicated Workspace authorities and the user-visible app barrier without creating a shared domain monolith; single-Experience distributions use the same topology with fewer installed Modules.
