# Coordinate applications through Workspace identity

A Workspace is the common cross-application container. The Workspace Directory owns only identity, display metadata, enabled modules, and module bindings; each application owns and persists its own Workspace shard. Standalone deployments use a local directory, so the suite is never a runtime dependency for an active Samsinn room or Leitbild Simulation Run.
