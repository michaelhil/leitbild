# Accelerated copies are ordinary Simulation Runs

An accelerated experiment is created as an independent ordinary Simulation Run at a coherent checkpoint, then advanced through the same Pack clock boundary without wall-time pacing. It is not a speed multiplier on the source, a second runtime type, or a state-skipping solver path; live external runtimes must be captured by an explicit evidence policy or reject acceleration rather than pretending future observations are simulated.
