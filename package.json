{
  "name": "samsinn-leitbild-platform",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.4.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "check:boundaries": "bun run scripts/check-boundaries.ts",
    "check:contracts": "bun run --cwd packages/platform-contracts check",
    "check:samsinn": "bun run --cwd apps/samsinn check",
    "check:leitbild": "bun run --cwd apps/leitbild check",
    "check:suite": "bun run --cwd apps/suite check",
    "check": "bun run check:boundaries && bun run check:contracts && bun run check:samsinn && bun run check:leitbild && bun run check:suite",
    "test:contracts": "bun run --cwd packages/platform-contracts test",
    "test:samsinn": "bun run --cwd apps/samsinn test:unit",
    "test:leitbild": "bun run --cwd apps/leitbild test",
    "test:suite": "bun run --cwd apps/suite test",
    "test": "bun run test:contracts && bun run test:samsinn && bun run test:leitbild && bun run test:suite"
  },
  "engines": {
    "bun": "1.4.0"
  },
  "devDependencies": {
    "bun-types": "1.4.0"
  }
}
